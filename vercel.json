{
  "version": 2,
  "builds": [
    {
      "src": "app.py", // Path to your Flask application entry point
      "use": "@vercel/python" // Use the Python runtime
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
